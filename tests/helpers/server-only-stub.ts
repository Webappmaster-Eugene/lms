/**
 * Заглушка пакета `server-only` для тестов.
 *
 * Сам пакет намеренно бросает исключение при импорте вне серверного окружения
 * Next — так он и защищает серверный код от попадания в клиентский бандл. Под
 * vitest этого окружения нет, поэтому в тестах он подменяется пустышкой
 * (см. resolve.alias в vitest.config.ts).
 */
export {}
