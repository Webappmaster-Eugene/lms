- `pnpm smoke` зелёный целиком (lint, typecheck, test, build)
- Схема коллекций и миграции сходятся; новая миграция экспортирована из
  `src/migrations/index.ts` и совместима с уже работающим кодом
- Новая переменная окружения добавлена в `.env.example`, `docker-compose.yml` и
  Dokploy UI; `NEXT_PUBLIC_*` прокинута как `ARG` в `Dockerfile`
- Изменения в `middleware.ts`, `src/payload/access/` и хуках Payload покрыты тестом:
  типы их не проверяют
- Выборки из Payload постраничные, без молчаливого лимита
- Секреты не попали ни в код, ни в логи, ни в отчёты
- Тексты для пользователя по-русски и без англицизмов
- В коммитах нет чужой незакоммиченной работы
